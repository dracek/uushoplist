import UuShoplist from "uu_shoplist_maing01-hi";
import { mount, wait } from "uu5g05-test";

describe(`UuShoplist.Bricks.Home.ShopListDataProvider`, () => {
  // it("onLoad", async () => {
  //   let onLoad = jest.fn(async () => ({ content: "Hello, world!" }));
  //   let loadDtoIn = { foo: "bar" };
  //   let wrapper = mount(<UuShoplist.Bricks.Home.ShopListDataProvider onLoad={onLoad} loadDtoIn={loadDtoIn} />);
  //   expect(onLoad).toHaveBeenCalledTimes(1);
  //   expect(onLoad).lastCalledWith(loadDtoIn);
  //   await wait();
  //   expect(wrapper.text()).toContain("Hello, world!");
  // });
});
